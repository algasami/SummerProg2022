{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "lldb.exe build and debug active file",
			"type": "lldb",
			"request": "launch",
			"program": "${fileDirname}/${fileBasenameNoExtension}.out",
			"args": [],
			"cwd": "${workspaceFolder}",
			"preLaunchTask": "g++ build active file"
		}
	]
}
